eureka:
  instance:
    hostname: localhost
  client:
    register-with-eureka: off
    fetch-registry: off
    service-url:
      default-zone: http://${eureka.instance.hostname}:${server.port}/eureka/

server:
  port: 8761
